<%= render "devise/shared/auth_shell",
           title: "Recover access",
           subtitle: "Reset your credentials quickly and get back to your incident queue." do %>
  <h2 class="auth-form-title">Forgot your password?</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, data: { turbo: false } }) do |f| %>
    <div class="space-y-4">
      <div>
        <%= f.label :email, class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true, class: "form-input" %>
      </div>

      <div>
        <%= render "devise/shared/turnstile" %>
      </div>

      <div class="pt-2">
        <%= f.submit "Send reset instructions", class: "btn-primary w-full" %>
      </div>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
<% end %>
