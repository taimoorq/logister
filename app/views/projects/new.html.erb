<section class="max-w-xl mx-auto">
  <div class="rounded-xl bg-white border border-slate-200 shadow-sm p-6 sm:p-8">
    <h1 class="text-2xl font-bold text-slate-900 mb-2 inline-flex items-center gap-2"><%= app_icon(:plus, css: "w-6 h-6") %><span>Create a project</span></h1>
    <p class="text-slate-600 text-sm mb-6">A project represents one monitored application and its integration setup.</p>

    <%= form_with model: @project do |f| %>
      <div class="space-y-4">
        <div>
          <%= f.label :integration_kind, "Integration", class: "form-label" %>
          <%= f.select :integration_kind, Project.integration_options, {}, class: "form-input" %>
          <p class="text-slate-500 text-xs mt-1">Choose how this project will send telemetry into Logister.</p>
        </div>

        <div>
          <%= f.label :name, class: "form-label" %>
          <%= f.text_field :name, required: true, class: "form-input" %>
        </div>

        <div>
          <%= f.label :slug, class: "form-label" %>
          <%= f.text_field :slug, placeholder: "my-production-app", class: "form-input" %>
        </div>

        <div>
          <%= f.label :description, class: "form-label" %>
          <%= f.text_area :description, rows: 4, class: "form-input" %>
        </div>

        <% if @project.errors.any? %>
          <div class="rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-800">
            <%= @project.errors.full_messages.to_sentence %>
          </div>
        <% end %>

        <div class="pt-2">
          <%= f.button class: "btn-primary inline-flex items-center gap-2" do %>
            <%= app_icon(:upload) %><span>Create project</span>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</section>
