This is the <%= @delivery %> delivery test from your Logister installation.

If you received this message, the configured SMTP path accepted it. Return to Admin → Installation → Email to review the verification status.
