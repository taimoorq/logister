<div id="api_key_new_token"
     class="rounded-lg bg-blue-50 border border-blue-200 px-4 py-3 text-sm text-blue-800 mb-4"
     style="view-transition-name: api-key-message; view-transition-class: api-key-message;">
  New API token: <code class="font-mono text-xs break-all"><%= token %></code> — copy it now; it will not be shown again.
</div>
