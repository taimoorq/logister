---
schema_version: 1
id: published-addon-catalog
release_version: 3.6.12
summary: Publish verified SDK versions and CLI 1.1.0 recommendation. After verifying
  deployed Rails 3.6.12 and all public channels, promote its catalog and Docker examples;
  existing SDK versions and contract digests remain valid.
backend:
  bump: patch
  compatibility: compatible
  activation: after_consumers
contracts:
- cli_api
- telemetry_ingest
- integration_discovery
contract_sha256:
  cli_api: e48ce2024a14419eef344855b84f4999537e8a4bf2d14c7ad65a4ea830f0a06c
  telemetry_ingest: bbb9a8d88aa0cb7632e3fe8b3c06fb18ea2f41b6209aeb20194d900827bb06e0
  integration_discovery: 58837a3dda7f3a6d145cd75aaf8060bdb783890c12a2b9efcec8aafe8cc7c00f
consumers:
  logister-cli:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-ruby:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-js:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-python:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-dotnet:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-android:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
  logister-ios:
    bump: none
    reason: Already published compatible SDKs and CLI remain valid; only release catalog
      metadata and the CLI recommendation change.
