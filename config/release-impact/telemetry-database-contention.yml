schema_version: 1
id: telemetry-database-contention
release_version: "3.6.5"
summary: Reduce telemetry counter and project locking while preserving atomic acceptance, retry identity, and purge ordering.
backend:
  bump: patch
  compatibility: compatible
  activation: immediate
contracts:
  - cli_api
  - telemetry_ingest
contract_sha256:
  cli_api: f24520a1fb4883d31fd81d8f0953f06c55a01db62a1cfe2c87b53f24bdf6d85b
  telemetry_ingest: bb2ef756755313d1229bab8d16ed80ddd0778328affcf57002c235b7d4cfb9fd
consumers:
  logister-ruby:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter Ruby SDK ingestion.
  logister-js:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter JavaScript SDK ingestion.
  logister-python:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter Python SDK ingestion.
  logister-dotnet:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter .NET SDK ingestion.
  logister-android:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter Android ingestion or evidence contracts.
  logister-ios:
    bump: none
    reason: Telemetry counter and lock changes are internal and does not alter iOS ingestion or evidence contracts.
  logister-cli:
    bump: none
    reason: The CLI API shape is unchanged; only compatible backend release identity metadata advances.
